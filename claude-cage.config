-- Test config for claude-cage-git
claude_cage {
    user = "test-cage-user",
    project = "test-project",
    showBanner = true,
    networkMode = "allowlist",

    -- Gitignore-style exclude patterns
    exclude = {
        ".git",
        "**/.env",
        "**/node_modules",
        "secrets/**",
    },

    allow = {
        domains = { "github.com:443", "api.anthropic.com:443" },
    },
}
