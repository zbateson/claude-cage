-- Example project-specific configuration for a frontend project
-- This file demonstrates how to override workspace settings for a specific project
--
-- Usage in a multi-project workspace:
--   1. Create claude-cage.config at parent directory with shared settings
--   2. Create frontend.claude-cage.config (this file) with frontend-specific overrides
--   3. cd into the frontend directory and run: cd frontend && sudo claude-cage
--
-- The project name "frontend" is derived from the directory structure.
-- This file is automatically loaded because you're running from the frontend/ subdirectory.
--
-- The settings in this file will be merged with claude-cage.config,
-- with this file's settings taking precedence for simple values,
-- and arrays being merged together.

claude_cage {
    -- Source is derived from config root - no need to specify
    -- The script knows you're in the frontend/ subdirectory

    -- Frontend-specific file excludes (merged with workspace excludes)
    exclude = {
        name = {
            "node_modules",     -- npm dependencies
            "dist",             -- Build output
            "build",            -- Alternative build output
            ".next",            -- Next.js build cache
            "coverage"          -- Test coverage reports
        }
    },

    -- Frontend-specific network allowances
    -- Uncomment to allow access to dev server
    -- allowedIPs = {
    --     "127.0.0.1:3000",   -- React/Next.js dev server
    --     "127.0.0.1:5173",   -- Vite dev server
    --     "127.0.0.1:8080"    -- Webpack dev server
    -- }
}
